# Смена даты у старых новостей на проде

**Дата инцидента:**  2026-09-03

## Описание
При деплое нового функционала для закрепления новостей произошел инцидент, при котором у всех старых новостей обновилась дата на текущую (2026-09-03).

Для добавления этого функционала было добавлено новое поле `isPinned` в новости. После добавления этого поля у всех старых новостей в базе данных в качестве значения был проставлен `null`. Но для корректной сортировки новостей значение должно быть либо `false`, либо `true`. Для этого был написал скрипт, который находил все записи с `isPinned=null` и менял их на `false`.

```js
const newsList = await strapi.db.query('api::news-collection.news-collection')
    .findMany({
      select: [
        'id',
        'isPinned'
      ]
    });

  for (const news of newsList) {
    if (news.isPinned === null) {
      await strapi.db.query('api::news-collection.news-collection').update({
        where: {
          id: news.id
        },
        data: {
          isPinned: false
        }
      });
    }
  }
```

Этот код бы хорошо сработал, если бы у нас не было кастомного кода для lifecycles, в котором содержится логика для автоматического проставления сегодняшней даты, если она не была проставлена вручную.

Не было учтено, что входящие параметры в `beforeCreate` и `beforeUpdate` — это параметры из request body запроса, а в скрипте для установки дефолтного значения `isPinned` были переданы только два параметра `id` и `isPinned`. Поэтому в `beforeUpdate` параметр `date` не приходил и срабатывала установка текущей даты, что и привело к обновлению дат в старых новостях.

```js
module.exports = {
  beforeCreate({ params }) {
    const { data } = params;

    generateSlug(data)
  },

  beforeUpdate({ params }) {
    const { data } = params;

    generateSlug(data)
  },
}

function generateSlug(data: any) {
  if (!data.date) {
    data.date = new Date();
  }

  ...
}
```

## Фикс кода
Исправленный вариант передаёт все поля в `beforeUpdate`, поэтому дата не перезаписывается.

```js
  await strapi.db.query('api::news-collection.news-collection').updateMany({
    where: {
      isPinned: null
    },
    data: {
      isPinned: false
    }
  });
```

## План по восстановлению дат 
План по восстановлению:
1. Откатываем image tag pelican-ui (это нужно, чтобы при откате версии CMS страница новостей корректно работала).
2. Откатываем image tag pelican-cms.
3. Накатываем бэкап БД за 03.09.2026.
4. Мерджим фикс кода в мастер pelican-cms.
5. Вручную триггерим деплой pelican-cms и pelican-ui.
